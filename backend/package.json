{
  "name": "vet-clinic-azure",
  "version": "1.0.0",
  "description": "Azure Functions for Vet Clinic Application",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "test": "echo \"No tests yet\""
  },
  "dependencies": {
    "mongodb": "^4.5.0"
  },
  "devDependencies": {
    "@azure/functions": "^3.0.0",
    "@types/node": "^16.0.0",
    "typescript": "^4.0.0"
  }
}